# Audit Log — completed milestones

Archived from [2026-07-20-audit-log.md](2026-07-20-audit-log.md). Append-only.

---

## Milestone 1: Audit entity and service — delivered 2026-07-22

**Size:** S
**Goal:** An append-only audit table exists and can be written to and read from in code.
**Depends on:** none

### Ticket 1.1: Add AuditLog entity

- **Status:** done
- **Size:** XS
- **Complexity:** trivial
- **Agent tier:** `haiku`
- **Escalation:** none
- **Depends on:** none
- **Needs approval:** none
- **Files:**
  - Create: `src/audit/audit-log.entity.ts`
  - Test: `src/audit/audit-log.entity.spec.ts`

**Context:** A TypeORM entity mirroring the shape already used by `src/cases/case.entity.ts` — `@Entity`, uuid primary key, `@CreateDateColumn`. Columns: `actorId` (uuid), `caseId` (uuid, indexed), `action` (varchar), `payload` (jsonb, nullable). No updates or deletes are ever issued against it in application code.

**Acceptance criteria:**

- [x] Entity compiles and is picked up by the existing TypeORM autoload glob
- [x] `caseId` is indexed
- [x] `payload` accepts null

**Definition of done:**

- [x] Acceptance criteria met
- [x] `testing-gate` run over the test files above
- [x] `qoq gate <files above>` → PASS
- [x] Change committed; hash recorded in **Commit** below
- [x] Status set to `done`; advisories (if any) noted below

**Advisories:** none

**Commit:** `a1b2c3d`

### Ticket 1.2: Add AuditLogService

- **Status:** done
- **Size:** S
- **Complexity:** mechanical
- **Agent tier:** `haiku`
- **Escalation:** none
- **Depends on:** Ticket 1.1
- **Needs approval:** none
- **Files:**
  - Create: `src/audit/audit-log.service.ts`
  - Create: `src/audit/audit.module.ts`
  - Test: `src/audit/audit-log.service.spec.ts`

**Context:** Injectable service wrapping the `AuditLog` repository, following the constructor-injection shape in `src/cases/cases.service.ts`. Named `record(actorId, caseId, action, payload)` rather than `log()`, to avoid reading as a logger call at the call site. Module exports the service so other modules can import it.

**Acceptance criteria:**

- [x] `record()` persists one row with the given fields
- [x] `AuditModule` exports `AuditLogService`
- [x] No update or delete method is exposed

**Definition of done:**

- [x] Acceptance criteria met
- [x] `testing-gate` run over the test files above
- [x] `qoq gate <files above>` → PASS
- [x] Change committed; hash recorded in **Commit** below
- [x] Status set to `done`; advisories (if any) noted below

**Advisories:** none

**Commit:** `e4f5a6b`

### Milestone 1 — Definition of done

- [x] All tickets above are `done`
- [x] Full quality suite: `qoq gate` (no explicit paths, full milestone scope) → PASS
- [x] Project's full build + full test suite green (not the scoped commands)
- [x] Milestone archived
